# Fenced With Tildes

A README using `~~~` fences instead of backticks:

~~~html
<img src="assets/logo.png" alt="TildeFence" width="400">
~~~

Tilde fences are valid CommonMark. The parser must treat them the same as
backtick fences — the `<img>` above is documentation.
