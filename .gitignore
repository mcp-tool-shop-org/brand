node_modules/
dist/
*.tsbuildinfo
coverage/
*.log
.DS_Store
.env
.env.local
site/.astro/
site/dist/
site/node_modules/
.polyglot-cache.json
swarms/

# Local repo-knowledge scaffolding (Phase 5 of full-treatment). rk.config.json
# is unfilled local tooling config (placeholder owner) and data/ is its SQLite
# DB + artifacts sink — neither belongs in git history, and both were otherwise
# committable by a blanket `git add -A` during release.
data/
rk.config.json

PLAN.md
collect-logos.sh
migrate-readmes.sh
push-all.sh
revert-all.sh
.swarm/

# Generated by the model-passthrough Astro integration: logos/<slug>/model/*
# staged for the build. Never hand-edited, never committed.
site/public/model/
